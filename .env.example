# Only needed for the Vercel-hosted frontend build, pointing it at the
# separately-hosted backend (Render/Railway/etc). Leave unset for local dev —
# Vite's dev server proxies /socket.io to localhost:3001 automatically.
VITE_SERVER_URL=https://your-backend.onrender.com
